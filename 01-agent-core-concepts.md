# AI Agent 学习笔记

> 基于 [AI Agent Book 第一章](https://bojieli.github.io/ai-agent-book/book/chapter1/) 系统化梳理
>
> 整理日期：2026-07-16

---

## 目录

1. [Agent 核心公式](#1-agent-核心公式)
2. [ReAct 循环](#2-react-循环)
3. [上下文五组件与消融实验](#3-上下文五组件与消融实验)
4. [Harness 工程](#4-harness-工程)
5. [编排模式：工作流 vs 自主 Agent](#5-编排模式工作流-vs-自主-agent)
6. [人工干预（Human in the Loop）](#6-人工干预human-in-the-loop)
7. [全章总结](#7-全章总结)

---

## 1. Agent 核心公式

**Agent = LLM + 上下文 + 工具**

更直观的说法：**大脑 + 眼睛 + 手脚**。

### 三大组件的广义理解

| 组件 | 比喻 | 广义含义 |
|------|------|---------|
| **LLM** | 大脑 | 不只是模型参数，而是整个决策内核——理解意图、思考规划、做出判断。能力来自两部分：**预训练**积累的世界知识 + **后训练**固化的决策策略 |
| **上下文** | 眼睛 | 不只是输入文本，而是每个决策点能看到的全部信息——环境、记忆、知识、自身状态和任务进展 |
| **工具** | 手脚 | 不只是 API 函数，而是能做的所有事——预定义工具调用、按需加载技能(Skills)、动态生成代码、委托子Agent、与用户沟通 |

### 关键洞察

章节引用了 Hennessy & Patterson 的 ISA（指令集体系结构）概念：

> **观察空间 + 动作空间 = LLM 与外部世界的接口**

模型固定时，提升 Agent 能力最有效的手段就是**扩展这两个空间**。

---

## 2. ReAct 循环

三大组件如何协同工作？答案就是 **ReAct 循环**（Reasoning + Acting）。

### 三步循环

```
思考(Reason) → 行动(Act) → 观察(Observe) → 思考(Reason) → ...
```

- **思考**：LLM 理解当前状态，决定下一步
- **行动**：调用工具执行操作
- **观察**：获取工具返回的结果，更新认知

### 上下文的累积性

每次调用 LLM 都能看到**完整轨迹**（之前所有的消息、回复和工具结果），这让 Agent 知道"我现在做到哪了"。

```
上下文 = 静态前缀 + 轨迹
```

- **静态前缀**：系统提示词 + 工具定义（不变）
- **轨迹**：用户消息 + 模型回复（含思考和工具调用） + 工具结果（持续增长）

### 实例

多币种收入汇总场景：
- 第1轮：并行调用3个货币转换工具
- 第2轮：调用代码解释器计算
- 第3轮：输出最终答案

仅 **3次迭代、4次工具调用** 即完成任务。

---

## 3. 上下文五组件与消融实验

章节通过**消融实验**验证了上下文五个组件各自不可替代。

### 五大组件

| # | 组件 | 去掉后的影响 |
|---|------|-------------|
| 1 | **系统提示词** | Agent 失去角色定位和基本行为准则 |
| 2 | **工具定义** | Agent 不知道有哪些工具可用，完全丧失行动能力 |
| 3 | **思考过程** | 前后决策互相矛盾，丧失思维连贯性 |
| 4 | **历史消息** | 失忆，从头开始整个任务，重复已完成步骤 |
| 5 | **工具结果** | 看不到上一步反馈，反复调用同一工具，陷入无限循环 |

### 核心洞察

> **上下文决定 Agent 能看到什么，而 Agent 只能基于看到的信息做决策。**

消融实验像医生逐一排除病因——每次去掉一个组件看系统是否还正常：

- 去掉**工具定义** → 完全丧失行动能力
- 去掉**工具结果** → 陷入无限循环
- 去掉**思考过程** → 前后决策矛盾
- 去掉**历史消息** → 失忆重复已完成步骤

---

## 4. Harness 工程

这是整章最重要的概念转折——从"Agent 是什么"转向"Agent 如何在生产环境可靠运行"。

### 定义

**Harness = 模型之外的所有工程实践**

一个 Agent 在生产环境可靠运行，除了 LLM 本身，还需要：

| 层 | 要素 | 说明 |
|----|------|------|
| 1 | **上下文** | 什么时候给模型看什么信息 |
| 2 | **工具** | 给模型哪些能力，约束怎么调 |
| 3 | **约束** | 防止模型做不该做的事 |
| 4 | **验证** | 模型做完后如何检查结果是否正确 |
| 5 | **纠正** | 检查发现问题时如何修复 |

五要素形成闭环：上下文 → 工具 → 约束 → 验证 → 纠正 → 重新回到上下文

### 关键判断

> **当各家模型能力越来越接近，竞争优势就转移到了模型之外的工程实践。**

实证：LangChain 在 Terminal Bench 2.0 上从 52.8% 提升到 66.5%，改变的不是模型，而是 Harness——让 Agent 自动检查执行结果、检测循环、优化思考策略。

### 工程范式演进链

```
软件工程 → 提示工程 → 上下文工程 → Harness 工程 → Loop 工程 → Graph 工程
```

每一层都是前一层的**超集**，而非替代品。

---

## 5. 编排模式：工作流 vs 自主 Agent

这是做 Agent 系统时最先面对的架构决策：用确定性流程还是动态决策？

### 两种模式对比

| 维度 | 工作流（Workflow） | 自主 Agent（Autonomous） |
|------|-------------------|-------------------------|
| **决策方式** | 确定性路由，预定义步骤 | LLM 动态决策，灵活选择路径 |
| **可靠性** | 高，可控可预测 | 低，存在幻觉和偏离风险 |
| **灵活性** | 低，难以处理未预见情况 | 高，能应对新颖场景 |
| **适用场景** | 合规流程、标准操作 | 探索性任务、复杂决策 |
| **调试难度** | 低，路径明确 | 高，行为不可预测 |

### 核心原则

> **从简单到复杂：先优化提示词 → 再考虑工作流 → 最后才引入自主 Agent**

如果优化提示和上下文示例就能解决，就别引入 Agent 系统。

### 实践建议

两种模式常常**混合使用**：
- 关键合规流程 → 工作流保证可靠性
- 灵活决策部分 → 切换到自主模式

---

## 6. 人工干预（Human in the Loop）

> ⚡ 这是 URL `#_8` 锚点对应的小节

人工干预是 Agent 安全运行的**最后一道防线**。

### 核心价值

让 Agent **"在无法完成时优雅地转移控制权"**，而不是直接报错或继续死磕。

### 两种触发条件

| 触发条件 | 说明 | 示例 |
|----------|------|------|
| **超过失败阈值** | 重试次数设上限，多次尝试仍无法理解意图 → 升级人工 | 用户意图模糊，Agent 反复理解失败 |
| **高风险操作** | 敏感、不可逆的操作 → 必须人工确认 | 取消订单、大额退款、付款审批 |

### 部署早期的特殊价值

在 Agent 部署早期尤为重要，有助于：
- **识别失败模式**——哪些场景 Agent 容易出错
- **发现边缘情况**——训练数据没覆盖的特殊场景
- **建立评估周期**——逐步量化 Agent 的可靠度

### 设计原则

1. 设定明确的**重试上限**（如3次），超过即升级人工
2. 对**不可逆操作**强制人工确认
3. 保留完整的**决策轨迹**，方便人工快速理解上下文
4. 随着系统成熟，逐步**收窄人工干预范围**

---

## 7. 全章总结

| # | 知识点 | 一句话总结 |
|---|--------|-----------|
| 1 | **核心公式** | Agent = LLM(大脑) + 上下文(眼睛) + 工具(手脚)，三者缺一不可 |
| 2 | **ReAct循环** | 思考→行动→观察不断重复，上下文=静态前缀+轨迹(累积增长) |
| 3 | **上下文五组件** | 消融实验证明每个组件不可替代，上下文决定Agent能看到什么 |
| 4 | **Harness工程** | 模型之外=竞争力所在，五要素闭环：上下文+工具+约束+验证+纠正 |
| 5 | **编排模式** | 工作流(确定性) vs 自主(动态)，从简单到复杂，可混合使用 |
| 6 | **人工干预** | 两种触发：超过失败阈值 + 高风险操作，优雅移交控制权 |

### 终极洞察

> **模型还做不稳的，Harness 先补上；模型每内化一层，Harness 就卸下一层，转而兜底新的能力前沿。**

方向认同模型会持续吃掉 Harness，但节奏务实地看——此刻模型的能力边界就是 Harness 此刻的价值所在。

---

*本文基于 AI Agent Book 第一章内容整理，原文链接：[https://bojieli.github.io/ai-agent-book/book/chapter1/](https://bojieli.github.io/ai-agent-book/book/chapter1/)*
